import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Landmark } from "lucide-react";
import React from "react";
import type { CanonicalHedgeDataframes } from "../../../server/domain/dataframes";

const money = (value: number) => new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL", maximumFractionDigits: 2 }).format(value);
const pct = (value: number | null | undefined) => value === null || value === undefined ? "—" : `${new Intl.NumberFormat("pt-BR", { maximumFractionDigits: 4 }).format(value)}%`;

/** Consolida somente dívida CDI declarada e a equivalência nocional contratual já validada; não estima risco ou preço. */
export default function CdiDebtCoverageSummaryCard({ dataframes }: { dataframes: CanonicalHedgeDataframes }) {
  const debts = dataframes.economic_situation_dataframe.filter(situation => situation.situation_kind === "CDI_LINKED_DEBT" && situation.declared_currency === "BRL" && situation.indexer === "CDI");
  const totalDebt = debts.reduce((total, debt) => total + debt.declared_quantity, 0);
  const entries = debts.map(debt => {
    const alternative = dataframes.hedge_alternative_dataframe.find(row => row.economic_situation_id === debt.economic_situation_id && row.alternative_kind === "OTC_RATE_SWAP");
    const sizing = alternative ? dataframes.hedge_sizing_dataframe.find(row => row.alternative_id === alternative.alternative_id && row.sizing_status === "sized") : undefined;
    const referenceNotional = sizing?.coverage_target_pct === null || sizing?.coverage_target_pct === undefined ? null : debt.declared_quantity * sizing.coverage_target_pct / 100;
    return { debt, sizing, referenceNotional };
  });
  const coveredReference = entries.reduce((total, entry) => total + (entry.referenceNotional ?? 0), 0);

  return <Card className="mt-7 rounded-2xl border-[#d7e7e2] bg-white shadow-none" aria-label="Resumo de dívida CDI e cobertura contratual"><CardHeader className="border-b border-[#edf2f0] pb-4"><div className="flex items-start justify-between gap-3"><div><p className="text-[10px] font-semibold uppercase tracking-[.16em] text-[#6c8990]">Juros · exposição declarada</p><CardTitle className="mt-1 text-base text-[#17363e]">Dívida CDI e cobertura contratual</CardTitle></div><Landmark className="h-5 w-5 text-[#328a7a]" /></div><p className="mt-2 text-xs leading-5 text-[#607a76]">A visão reúne dívida CDI declarada e a referência nocional de swap de taxa já validada. Ela não mede duration, sensibilidade, taxa, cupom, curva, MTM, DV01 ou resultado financeiro.</p></CardHeader><CardContent className="p-5">{debts.length === 0 ? <p className="rounded-xl border border-dashed border-[#d8e5e2] bg-[#fbfdfc] px-4 py-5 text-center text-sm text-[#71878e]">Nenhuma dívida CDI declarada foi diagnosticada nesta sessão.</p> : <><div className="grid gap-3 sm:grid-cols-2"><div className="rounded-xl border border-[#dce8e5] bg-[#f8fbfa] p-4"><p className="text-[10px] font-semibold uppercase tracking-[.12em] text-[#6c8990]">Dívida CDI declarada</p><p className="mt-2 font-mono text-xl font-semibold text-[#17363e]">{money(totalDebt)}</p><p className="mt-1 text-[11px] text-[#71888d]">{debts.length} exposição(ões) com índice CDI.</p></div><div className="rounded-xl border border-[#dce8e5] bg-[#f8fbfa] p-4"><p className="text-[10px] font-semibold uppercase tracking-[.12em] text-[#6c8990]">Referência nocional coberta</p><p className="mt-2 font-mono text-xl font-semibold text-[#17363e]">{money(coveredReference)}</p><p className="mt-1 text-[11px] text-[#71888d]">Limitada à cobertura canônica de 100% por exposição.</p></div></div><div className="mt-4 overflow-x-auto"><table className="min-w-[760px] w-full text-left text-xs"><thead className="bg-[#f6fbf9] text-[10px] uppercase tracking-[.1em] text-[#69847f]"><tr><th className="px-3 py-3">Dívida / vencimento</th><th className="px-3 py-3 text-right">Nocional declarado</th><th className="px-3 py-3">Swap de taxa</th><th className="px-3 py-3 text-right">Cobertura nocional</th><th className="px-3 py-3">Estado</th></tr></thead><tbody className="divide-y divide-[#edf2f0]">{entries.map(({ debt, sizing }) => <tr key={debt.economic_situation_id} className="align-top text-[#456761]"><td className="px-3 py-3"><strong className="text-[#294a50]">{debt.description}</strong><br /><span className="font-mono text-[10px] text-[#71878e]">{debt.horizon_date}</span></td><td className="px-3 py-3 text-right font-mono">{money(debt.declared_quantity)}</td><td className="px-3 py-3">{sizing?.hedge_unit ?? "Não dimensionado"}</td><td className="px-3 py-3 text-right font-mono">{pct(sizing?.coverage_target_pct)}</td><td className="px-3 py-3"><Badge variant="outline" className={sizing ? "border-[#9cd6c8] bg-[#effbf7] text-[#20715f]" : "border-[#e8d1a6] bg-[#fff8ed] text-[#8d6740]"}>{sizing ? "NOCIONAL" : "PENDENTE"}</Badge></td></tr>)}</tbody></table></div><p className="mt-4 rounded-lg bg-[#fff8ed] p-3 text-[11px] leading-5 text-[#8d6740]">A referência contratual não substitui uma curva de juros ou a avaliação econômica do hedge. Sem convenções e insumos de mercado aprovados, os resultados quantitativos continuam bloqueados.</p></>}</CardContent></Card>;
}
