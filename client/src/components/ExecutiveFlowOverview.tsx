import { Badge } from "@/components/ui/badge";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { BarChart3, Database, FileCheck2, Landmark, ShieldCheck } from "lucide-react";
import React from "react";

type SourceStatus = { label: string; loaded: boolean; detail: string };

export default function ExecutiveFlowOverview({ exposureCount, alternativeCount, sourceStatuses }: { exposureCount: number; alternativeCount: number; sourceStatuses: SourceStatus[] }) {
  const loadedCount = sourceStatuses.filter(source => source.loaded).length;
  return <section className="mx-auto max-w-[1540px] space-y-6"><Card className="overflow-hidden rounded-3xl border-[#b8d8d0] bg-[linear-gradient(120deg,#12383e,#1e5b59)] text-white shadow-[0_24px_55px_-32px_rgba(17,61,67,.8)]"><CardContent className="grid gap-6 p-7 lg:grid-cols-[1.45fr_.9fr]"><div><Badge className="border-0 bg-white/10 px-3 py-1 text-[10px] font-semibold uppercase tracking-[.16em] text-[#b7eee0]">Fluxo de decisão de hedge</Badge><h2 className="mt-4 max-w-3xl text-3xl font-semibold tracking-[-.025em]">Comece pelo risco econômico. Termine com a evidência auditável.</h2><p className="mt-3 max-w-2xl text-sm leading-6 text-[#c6dddc]">Esta visão não pede importação nem exportação. Use a barra lateral na ordem das etapas para declarar a exposição, acompanhar fontes oficiais, avaliar alternativas e gerar o relatório final.</p></div><div className="grid grid-cols-2 gap-3"><div className="rounded-2xl border border-white/10 bg-white/[.07] p-4"><p className="text-[10px] uppercase tracking-[.14em] text-[#a8d8ce]">Fontes carregadas</p><p className="mt-2 text-3xl font-semibold">{loadedCount}/{sourceStatuses.length}</p><p className="mt-1 text-xs text-[#c6dddc]">Atualização automática na abertura</p></div><div className="rounded-2xl border border-white/10 bg-white/[.07] p-4"><p className="text-[10px] uppercase tracking-[.14em] text-[#a8d8ce]">Exposições</p><p className="mt-2 text-3xl font-semibold">{exposureCount}</p><p className="mt-1 text-xs text-[#c6dddc]">Declaradas nesta sessão</p></div><div className="col-span-2 rounded-2xl border border-white/10 bg-white/[.07] p-4"><p className="text-[10px] uppercase tracking-[.14em] text-[#a8d8ce]">Alternativas identificadas</p><p className="mt-2 text-2xl font-semibold">{alternativeCount}</p><p className="mt-1 text-xs text-[#c6dddc]">Dependem de dados oficiais ou termos contratuais antes de qualquer cálculo.</p></div></div></CardContent></Card><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-5"><Step icon={<Database />} step="1" title="Dados oficiais" detail="Acompanhe BCB, B3, ANBIMA, IBGE e FGV. Estados indisponíveis não recebem substitutos." /><Step icon={<Landmark />} step="2" title="Exposição" detail="Declare variável econômica, valor ou quantidade, unidade, vencimento e indexador." /><Step icon={<ShieldCheck />} step="3" title="Alternativas" detail="Veja instrumentos compatíveis e os dados ainda exigidos para cada um." /><Step icon={<BarChart3 />} step="4" title="Cenários" detail="Execute cenários didáticos separados das observações oficiais de mercado." /><Step icon={<FileCheck2 />} step="5" title="Relatório e sessão" detail="No módulo final, exporte o PDF e os pacotes explicados de auditoria." /></div><Card className="rounded-2xl border-[#cfe2dd] bg-white"><CardHeader className="pb-3"><CardTitle className="text-base text-[#17363e]">Situação das fontes na abertura</CardTitle></CardHeader><CardContent className="grid gap-3 sm:grid-cols-2 lg:grid-cols-5">{sourceStatuses.map(source => <div key={source.label} className={`rounded-xl border p-3 ${source.loaded ? "border-[#bde4d8] bg-[#f0fbf7]" : "border-[#e5d5b0] bg-[#fffaf0]"}`}><p className={`text-xs font-semibold ${source.loaded ? "text-[#176957]" : "text-[#765725]"}`}>{source.label}</p><p className="mt-1 text-[11px] leading-4 text-[#48676b]">{source.detail}</p></div>)}</CardContent></Card></section>;
}

function Step({ icon, step, title, detail }: { icon: React.ReactNode; step: string; title: string; detail: string }) {
  return <Card className="rounded-2xl border-[#d4e5e0] bg-white shadow-none"><CardContent className="p-4"><div className="flex items-center justify-between text-[#20725f]"><span className="text-[10px] font-bold tracking-[.14em]">ETAPA {step}</span><span className="h-4 w-4">{icon}</span></div><p className="mt-3 text-sm font-semibold text-[#17363e]">{title}</p><p className="mt-2 text-xs leading-5 text-[#4d6d72]">{detail}</p></CardContent></Card>;
}
